# pnpm — configuración para proyecto Electron.
# node_modules plano: electron-builder y @electron/rebuild esperan la estructura
# de npm para resolver y reconstruir módulos nativos (better-sqlite3).
node-linker=hoisted
